pipeline {
  agent any
  stages {
    stage('SCM checkout') {
      steps {
        echo 'scm checkout now...'
      }
    }

    stage('build') {
      steps {
        echo 'hi from build'
      }
    }

  }
}